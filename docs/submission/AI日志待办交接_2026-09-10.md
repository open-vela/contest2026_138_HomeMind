# AI Coding 日志 — 待办交接（2026-09-10）

> 给接手的 AI/队员：本文只列**尚未完成**的日志事项。已完成部分见下方「已就绪」。

## 已就绪（勿重做）

| 项 | 状态 |
| --- | --- |
| 官方采集器安装 | Ubuntu 完成：`~/.claude/contest-collector.env` → `TEAM_ID=contest2026_138_HomeMind`，`GITHUB_LOGIN=Miles-hfy` |
| verify-setup.sh | Passed 10 / Failed 0（2026-09-10） |
| 目录格式 | `logs/Miles-hfy/<date>/*.jsonl` + `manifest.json` |
| 官方校验 | `validate-log.py logs/` → **ALL OK**（6 files / 225 events） |
| 工具 | `contest-snapshot` 已在 `~/.local/bin/`（需 `export PATH="$HOME/.local/bin:$PATH"`） |
| 本地副本 | Windows `contest2026_138_HomeMind_official/logs/Miles-hfy/` 已同步 |

校验命令（Ubuntu，工作区 `~/work/openvela-clean-20260830/contest2026_138_HomeMind`）：

```bash
python3 ../.claude/skills/contest-log-collector/tools/validate-log.py logs/
```

## 待办 A：提交 logs 到专属仓远端

1. 确认无密钥：`grep -RiE 'sk-|password|psk |Bearer ' logs/Miles-hfy/ || true`
2. 在 Ubuntu 官方仓：

```bash
cd ~/work/openvela-clean-20260830/contest2026_138_HomeMind
git add logs/
git commit -s -m "logs: official schema Miles-hfy (6 sessions, validate ALL OK)"
# 推送按铁律：fork → PR → CLA → Rebase merge，禁止直推官方仓
```

3. 合规清单勾选：`docs/submission/HomeMind_作品提交合规清单.md` 第 4 节「git add/push」项。

## 待办 B：补 2026-09-01 至今 WorkBuddy / MiMo 会话

**现状**：`contest-snapshot --backfill --source all` 在 Ubuntu 导入 **0** 会话。原因：

- Windows MiMo/WorkBuddy 对话不在 Ubuntu `~/.claude/projects` 或 `.repo` 工作区内；
- 采集器**只在向上能找到 `.repo/` 的 openvela 工作区**内启用；
- 官方支持工具：Claude Code / OpenCode / Codex / MiMoCode（见 `event.schema.json` tool 枚举）。

**可选做法（按优先级）**：

1. **今后开发改在 Ubuntu openvela 工作区**用受支持工具，会话结束自动入 `logs/Miles-hfy/`（推荐，最干净）。
2. 若 Windows 有官方工具原始导出：拷到 Ubuntu 后用 `export-session.py --backfill --source sqlite` 或对应 adapter；**禁止手改 jsonl 字段伪造**。
3. 含密钥的会话：整文件删除，不入仓（与现有 12 份 withheld 同规则）。
4. 转换后的 jsonl 必须再跑 `validate-log.py`；manifest `sessions[].file_path` 与磁盘文件一一对应。

**不要**：把 09 月对话编造成 jsonl；不要编辑已通过校验的 6 份历史会话。

## 环境备忘

- 采集器 skill：`~/work/openvela-clean-20260830/.claude/skills/contest-log-collector/`
- 手动列表：`python3 .../tools/export-session.py --list`
- 补导：`... --backfill --confirm`（先 `--dry-run`）
- 原始 Codex 导出备份（仓外）：`/home/hfy/work/codex-raw-backup-20260910/`
- 合规清单：`docs/submission/HomeMind_作品提交合规清单.md` §4

## 边界

历史 6 份会话日期仅 **2026-07-28 ~ 08-30**，不能代表 09 月摄像头/TFLM/混合检测开发过程；报告中 AI Coding 占比须按实际日志统计，勿夸大覆盖。

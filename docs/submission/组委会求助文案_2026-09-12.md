# 给官方群的求助文案（2026-09-12 更新，直接复制）

> 现状：PR #1 已开且可合并性为 blocked；`cla/signature` 为 Required，但状态一直是
> “Waiting for status to be reported”。已评论 `/check-cla`，全仓 Actions runs 仍为 0。
> Rebase and merge 按钮灰掉，不是权限问题，是必需检查未上报。

---

## 一、直接复制发群

```
老师好，138 队 HomeMind 再次求助官方仓 CLA 检查未运行：

PR：https://github.com/open-vela/contest2026_138_HomeMind/pull/1
base：dev-ai-contest-2026（当前仍是模板 961cf68）
head：Miles-hfy/contest-final @ 73baee3026d2ce13cfbf4e180a84f59ce5886cd5
变更：52 commits / 300 files

现象：
1）PR 状态 Checks pending，必需检查 cla/signature 显示
   Expected — Waiting for status to be reported
2）Miles-hfy 已在 openvela.com 签署 CLA，并在 PR 评论 /check-cla（约 23 分钟前）
3）官方仓 Actions 总运行数仍为 0，没有任何 workflow run
4）Rebase and merge 按钮灰掉，无法合入

我们不是操作问题：规则集要求 cla/signature，但该检查从未上报。
请老师帮忙：
A. 检查 open-vela/contest2026_138_HomeMind 是否禁用了 Actions
   （Settings → Actions → General → Allow all actions）
B. 或帮忙触发/通过 cla/signature
C. 或在检查异常时协助 Rebase merge 该 PR

源码、日志、证据已在 fork contest-final（最新 73baee3），合入后我们会从官方分支复核文件。
谢谢老师！
```

---

## 二、证据要点

| 项 | 当前值 |
| --- | --- |
| 官方分支 HEAD | `961cf680946773cd4c1f41b29c425de892bd9a69`（模板） |
| PR head | `73baee3026d2ce13cfbf4e180a84f59ce5886cd5` |
| mergeable | true，但 mergeable_state = **blocked** |
| rebaseable | false（被 required check 挡住） |
| combined status | pending，statuses 列表为空 |
| check-runs | 0 |
| Actions runs | 0 |
| 必需检查 | `cla/signature`（ruleset 18607770） |
| 允许合并方式 | 仅 rebase |
| CLA 评论 | `/check-cla` 已发，检查仍未上报 |
| PAT API 合并 | 403 Resource not accessible by personal access token |

## 三、若你能在浏览器看到 Settings

`Settings` → `Actions` → `General` → `Actions permissions`  
选 **Allow all actions and reusable workflows** → Save  
然后再次评论 `/check-cla`。

看不到 Settings 或改不了就只能等组委会。

## 四、Actions/检查恢复后的操作

1. 刷新 PR，等 `cla/signature` 变绿  
2. 点 **Rebase and merge**（不要 Squash / merge commit）  
3. 合入后从官方分支确认文件与 `73baee3` 内容一致  

# 2026-08-31 ESP32-S3-EYE 真机验收（脱敏记录）

- 固件：干净工作区构建产物 BIN `902ccdaa2109d8a2b1ecc8d5247b58d4788078eb4689e7390ee4124ec115ef56`；ELF `c10e1e64bee0f989accd4dacf6fae6ce8327ead8d386aeda7b87835ab9fd8e87`。
- 刷写：Ubuntu `/dev/ttyACM1`，esptool v5.3.1，写入地址 `0x0`；设备识别为 ESP32-S3，写入后 `Hash of data verified`，hard reset 成功。
- Wi-Fi：使用用户提供的 SSID（密码不记录）；锁定最强 AP `50:88:11:7a:02:69`，`renew ret=0`，DHCP 获得 `192.168.31.248`；`net_status` 返回 `Network connected: yes`。
- HTTPS/TLS：`net_test www.baidu.com 443` 完整通过两次；TLS 1.2 `TLS-ECDHE-RSA-WITH-AES-128-GCM-SHA256`，HTTP 状态 `200`。第一次握手约 37.6 s，第二次约 0.33 s。
- 重启持久化：执行 `restart` 后设备回到 `vela>`，再次输入 `ai_agent` 返回 `Unknown command`（说明已在 agent 控制台），随后 `net_status` 仍为 connected，IP 仍为 `192.168.31.248`；证明 Wi-Fi 凭据可在重启后自动恢复。
- MiMo 首次验收：执行 `set_llm mimo <key>`（完整 key 不记录），`config_show` 显示 `sk-c****`、模型 `mimo-v2.5`、主机 `api.xiaomimimo.com`；`ask` 完成 TLS 1.2（CHACHA20-POLY1305，约 580 ms），HTTP `200`，回复包含 `homemind-mimo-ok`。
- MiMo 重启持久化：硬复位并等待网络稳定后，`config_show` 仍显示上述脱敏配置；未重新输入 key 的 `ask` 完成 TLS 1.2、HTTP `200`，回复包含 `homemind-mimo-restart-ok`。
- 本地 LED 工具：`ask` 打开灯返回 `{"led":"on"}`，关闭灯返回 `{"led":"off"}`；两次均为新构建固件上的快速路径实测。
- 冷启动连续性：5 轮 esptool 硬复位后，最终 `net_status` 均为 connected，DHCP 均获得 `192.168.31.248`，MiMo HTTP `200` 与唯一 token 均通过（5/5 PASS）。复位后第一次立即采样出现 `no/0.0.0.0`，随后 DHCP 完成，最终采样均为 `yes`。
- 受控重连连续性：3 轮硬复位并确认在线后执行 `wifi_reconnect`；三轮均报告内部 `ifdown_post_scan`、DHCP 恢复、最终 connected/IP，且 MiMo HTTP `200` 与唯一 token 均通过（3/3 PASS）。
- 断网入口诊断：在当前 `vela>` 控制台直接执行 `ifdown wlan0` 返回 `Unknown command`，故未将旧记录中的外部 `ifdown` 流程冒充为当前固件证据。
- 长稳观察（标准短回复）：单次硬复位后完成 10 轮，最终网络状态/IP 均正常；6/10 轮在 75 秒窗口内捕获 HTTP `200` 和最终 Agent 回复，4/10 轮只出现工作状态，未完成 HTTP/Agent 回复。失败轮次不伴随 DHCP/IP 丢失，当前记为 MiMo/Agent 请求链间歇超时，未宣称长稳通过。
- 自定义 token 对照：45 秒窗口为 5/10；延长至 75 秒的 3 轮均有 HTTP `200`，但模型未稳定复述指定 token。清空 `console` 会话后“只回复 OK”曾返回 HTTP `200` 与 `[Agent]: OK`，故不把自定义 token 复述失败冒充为 Wi-Fi 故障。

# 2026-09-01 TLS body framing 修复与暂停记录（脱敏）

- 修复正式 overlay 与 staging 副本：chunked response 识别终止块；未完成或超长 body 返回读取错误；HTTP 请求改为 `Connection: close`，避免复用 stale TLS socket。
- 干净构建产物：BIN `70fdae2e84b01587c87c143f2da99b912bb49dd2867a8fd80dfc883e7b76e87e`，ELF `2242cb231fef3987c2b444a4e3f10214b15d1143b33082bff2fff13124e16718`；SHA-256 自校验通过；esptool v5.3.1 写入校验通过。
- 冷启动 5 轮：每轮强 AP、DHCP `ret=0`、TLS 握手和 HTTP `200` 均通过；模型拒绝自定义 token，因此不把 0/5 token 语义结果当作传输失败。
- 标准自然语言连续回归：启动 PASS，第 1–7 轮均 `PASS`，每轮 HTTP `200`、Agent 回复、最终 IP `192.168.31.248`；用户要求暂停后停止后续轮次，未宣称 10/10。
- 证据文件：`logs/hardware-2026-09-01-stability-ok-partial.log`；完整 API key、Wi-Fi 密码和 SSH 密码未写入日志或仓库；远程未推送。
